README
    the read-repair strategy -> fsync-ing
    the read-repair strategy + client-side range caching
    the flake model
    document repo's organization
    polish

RR_SEQ
    implement fsync support
    implement timeouts & cancellations
    atomic query counter
    mayhem mode testing

TRAVIS
    yaml conf
    coveralls
